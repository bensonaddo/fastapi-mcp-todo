# Template only — never commit real secrets. In real clusters use
# ExternalSecrets/SealedSecrets or your cloud secret manager (see docs/deployment.md).
apiVersion: v1
kind: Secret
metadata:
  name: todo-api-secrets
  namespace: todo
type: Opaque
stringData:
  database-url: postgresql://todo:CHANGE_ME@postgres-host:5432/todos
